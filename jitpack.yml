# JitPack build configuration
version: 1.0
before_install:
  - echo "Preparing to build..."
install:
  - ./gradlew wrapper --gradle-version 7.5
  - ./gradlew clean build
script:
  - ./gradlew publish
